#include "game.hpp"

/**
 * Main game loop.
 */
int main() {
    //initialize board -> everything initialized will be moved prob
    GameManager gameManager{};

    gameManager.startGame();
    
    
    return 0;
}